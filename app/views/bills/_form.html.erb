<%= form_for(@bill) do |f| %>
  <% if @bill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bill.errors.count, "error") %> prohibited this bill from being saved:</h2>

      <ul>
      <% @bill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cent_value %><br />
    <%= f.text_field :cent_value %>
  </div>
  <div class="field">
    <%= f.label :household %><br />
    <%= f.select :household, options_for_select(@households) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
